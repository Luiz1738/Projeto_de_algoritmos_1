/*#include <iostream>
#include <math.h>

using namespace std;

int main() 
{
    int x, y, z;
    cin >> x;
    cin >> y;
    cin >> z;

    int maior = x;
    
    if(maior < y)
    {
        maior = y;
    }
    if(maior < z)
    {
        maior = z;
    }

    int menor = x;

    if(menor > y)
    {
        menor = y;
    }
    if(menor > z)
    {
        menor = z;
    }

    cout << maior << ", " << x + y + z - maior - menor << ", "<< menor << endl;
    
    return 0;
}
*/